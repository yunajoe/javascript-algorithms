# 스택(Stack)

## 1. 스택(Stack)이란?

### 정의

**스택(Stack)은 데이터를 쌓아 올리는 형태의 선형 자료구조로, 후입선출(LIFO, Last In First Out) 방식으로 동작한다.**
즉, 가장 마지막에 삽입한 데이터가 가장 먼저 제거된다.
접시를 쌓아 두는 모습을 생각하면 이해하기 쉬워. 가장 마지막에 올려놓은 접시를 가장 먼저 꺼내는 것과 같다.

### 스택의 주요 연산

| 연산           | 설명                              | 시간 복잡도 |
| -------------- | --------------------------------- | ----------- |
| `push`         | 스택의 맨 위에 데이터 삽입        | O(1)\*      |
| `pop`          | 스택의 맨 위 데이터 제거 및 반환  | O(1)\*      |
| `peek` / `top` | 맨 위 데이터를 제거하지 않고 확인 | O(1)        |
| `isEmpty`      | 스택이 비어 있는지 확인           | O(1)        |

\*일반적인 스택 구현 기준. 배열 기반 스택에서 `push`는 동적 배열의 용량 확장 시 간헐적으로 O(n)이 될 수 있지만, 일반적으로 분할 상환 O(1)이다.

### 스택의 동작 예시

```text
1. push(10)

   ┌────┐
   │ 10 │ ← Top
   └────┘

2. push(20)

   ┌────┐
   │ 20 │ ← Top
   ├────┤
   │ 10 │
   └────┘

3. push(30)

   ┌────┐
   │ 30 │ ← Top
   ├────┤
   │ 20 │
   ├────┤
   │ 10 │
   └────┘

4. pop()

   ┌────┐
   │ 20 │ ← Top
   ├────┤
   │ 10 │
   └────┘
```

`pop()`을 실행하면 가장 마지막에 삽입된 `30`이 제거돼.

### 스택의 활용 사례

- 함수 호출 관리(Call Stack)
- 괄호 짝 검사
- DFS(깊이 우선 탐색)
- 브라우저 뒤로 가기
- 실행 취소(Undo), 다시 실행(Redo)

## 2. 스택(Stack)과 배열(Array)의 차이

스택과 배열은 서로 대체되는 개념이 아니라, **배열은 데이터를 저장하는 자료구조이고 스택은 데이터를 관리하는 추상 자료형(ADT)** 이라는 차이.

배열을 이용해서 스택을 구현할 수도 있습니다.

### 핵심 비교

| 구분        | 배열(Array)                         | 스택(Stack)                                 |
| ----------- | ----------------------------------- | ------------------------------------------- |
| 개념        | 데이터를 순서대로 저장하는 자료구조 | LIFO 규칙으로 데이터를 관리하는 추상 자료형 |
| 접근 방식   | 인덱스를 통해 임의의 요소에 접근    | Top(맨 위)에서만 삽입·삭제                  |
| 데이터 삽입 | 인덱스 지정, 끝에 추가 등           | Top에 삽입 (`push`)                         |
| 데이터 삭제 | 인덱스 지정, 끝 요소 삭제 등        | Top에서 삭제 (`pop`)                        |
| 접근 제약   | 상대적으로 자유로움                 | 제한적                                      |
| 대표 연산   | `arr[i]`, `push`, `splice`          | `push`, `pop`, `peek`                       |

### JavaScript 예시

**배열(Array)**

```ts
const arr = [10, 20, 30];

// 인덱스로 임의의 요소 접근
console.log(arr[0]); // 10

// 중간 요소 삭제
arr.splice(1, 1);

console.log(arr); // [10, 30]
```

배열은 인덱스를 통해 원하는 위치의 데이터에 접근하거나 수정할 수 있습니다.

**스택(Stack)**

```ts
const stack: number[] = [];

stack.push(10);
stack.push(20);
stack.push(30);

console.log(stack.pop()); // 30
console.log(stack.pop()); // 20

console.log(stack); // [10]
```

배열을 스택처럼 사용하면 `push()`와 `pop()`으로 LIFO 규칙을 구현할 수 있습니다.

여기서 중요한 점은 **배열 자체가 스택인 것은 아니며, 배열에 스택의 규칙을 적용해서 스택처럼 사용하는 것**입니다.

## 3. 스택(Stack)과 콜 스택(Call Stack)의 차이

스택과 콜 스택은 서로 완전히 다른 자료구조가 아닙니다.

**콜 스택은 스택의 LIFO 구조를 활용해 프로그램의 함수 호출과 실행 흐름을 관리하는 메커니즘.**

일반적인 스택이 데이터의 삽입과 삭제를 관리한다면, 콜 스택은 함수가 호출될 때 실행 컨텍스트를 쌓고, 함수 실행이 끝나면 제거하는 방식으로 동작합니다.

### 핵심 비교

| 구분      | 스택(Stack)           | 콜 스택(Call Stack)                    |
| --------- | --------------------- | -------------------------------------- |
| 개념      | LIFO 방식의 자료구조  | 함수 호출 및 실행 흐름을 관리하는 스택 |
| 목적      | 데이터 관리           | 함수 실행 순서 및 실행 컨텍스트 관리   |
| 저장 대상 | 숫자, 문자열, 객체 등 | 함수 실행에 필요한 실행 컨텍스트       |
| 삽입 시점 | `push()` 호출 시      | 함수가 호출될 때                       |
| 제거 시점 | `pop()` 호출 시       | 함수 실행이 완료될 때                  |
| 대표 사례 | DFS, 괄호 검사, Undo  | 함수 호출, 재귀 함수, 스택 오버플로    |

### 콜 스택의 동작 원리

```ts
function first() {
  second();
  console.log("first");
}

function second() {
  third();
  console.log("second");
}

function third() {
  console.log("third");
}

first();
```

함수는 `first()` → `second()` → `third()` 순서로 호출.

### 1단계: first() 호출

```text
┌─────────────┐
│ first()     │ ← Top
└─────────────┘
```

콜 스택에 `first()`의 실행 컨텍스트가 쌓입니다.

### 2단계: second() 호출

`first()`가 실행되는 도중 `second()`를 호출합니다.

```text
┌─────────────┐
│ second()    │ ← Top
├─────────────┤
│ first()     │
└─────────────┘
```

`second()`의 실행 컨텍스트가 `first()` 위에 쌓입니다.

### 3단계: third() 호출

`second()`가 `third()`를 호출합니다.

```text
┌─────────────┐
│ third()     │ ← Top
├─────────────┤
│ second()    │
├─────────────┤
│ first()     │
└─────────────┘
```

이제 `third()`가 가장 위에 있습니다.

### 4단계: 함수 실행 종료 및 제거

`third()`가 끝나면 콜 스택에서 제거되고, `second()`의 실행이 이어집니다.

```text
third() 종료 → third 컨텍스트 제거
second() 종료 → second 컨텍스트 제거
first() 종료 → first 컨텍스트 제거
```

```text
third
second
first
```

**핵심은 함수 호출은 쌓이는 순서대로, 함수 종료는 마지막에 호출된 함수부터 역순으로 진행된다는 것**

### 콜 스택과 JavaScript의 관계

JavaScript는 실행 중인 함수의 실행 컨텍스트를 콜 스택으로 관리

- 함수가 호출되면 콜 스택에 실행 컨텍스트가 쌓임
- 함수 실행이 끝나면 해당 실행 컨텍스트가 제거됨
- 콜 스택이 처리 중인 작업은 기본적으로 한 번에 하나씩 실행됨
- 재귀 호출이 지나치게 깊어지면 `Maximum call stack size exceeded` 오류가 발생할 수 있음

예를 들어, 종료 조건이 없는 재귀 함수는 콜 스택을 계속 쌓다가 스택 오버플로(Stack Overflow)를 일으킬 수 있습니다.

```ts
function recursive() {
  recursive();
}

recursive();
```

## 4. 최종 핵심 정리

| 개념                | 한 줄 정의                                   | 핵심 키워드                      |
| ------------------- | -------------------------------------------- | -------------------------------- |
| 스택(Stack)         | 후입선출 방식으로 데이터를 관리하는 자료구조 | LIFO, Push, Pop                  |
| 배열(Array)         | 인덱스를 통해 데이터에 접근하는 자료구조     | Index, Random Access             |
| 콜 스택(Call Stack) | 함수 호출과 실행 컨텍스트를 관리하는 스택    | Function Call, Execution Context |

### 면접 대비 핵심 문장

> 스택은 LIFO 방식으로 동작하는 추상 자료형이며, 배열은 스택을 구현하는 데 사용할 수 있는 자료구조입니다. 콜 스택은 스택의 구조를 활용해 함수 호출과 실행 컨텍스트를 관리하며, 함수가 호출될 때 쌓이고 실행이 완료되면 제거됩니다.

**기억할 것:** 배열은 데이터를 어떻게 저장하고 접근할지에 관한 구조이고, 스택은 데이터를 어떤 규칙으로 삽입하고 제거할지에 관한 개념이야. 콜 스택은 그 스택 구조를 함수 실행 관리에 활용한 것이지.

```javascript
class Stack {
  constructor() {
    // 빈 스택 하나를 생성합니다.
    this.items = [];
  }

  push(item) {
    // 스택에 데이터를 추가합니다.
    this.items.push(item);
  }

  empty() {
    // 스택이 비어있으면 true를 반환합니다.
    return this.items.length === 0;
  }

  size() {
    // 스택에 있는 데이터 수를 반환합니다.
    return this.items.length;
  }

  pop() {
    // 스택의 가장 위에 있는 데이터를 반환하고 제거합니다.
    if (this.empty()) {
      throw new Error("Stack is empty");
    }

    return this.items.pop();
  }

  top() {
    // 스택의 가장 위에 있는 데이터를 제거하지 않고 반환합니다.
    if (this.empty()) {
      throw new Error("Stack is empty");
    }

    return this.items[this.items.length - 1];
  }
}
```
