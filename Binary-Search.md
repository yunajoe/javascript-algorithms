## 이진 탐색 개념

- **정의**: 오름차순이나 내림차순으로 **정렬되어 있는 리스트**에서 범위를 반씩 줄여가며 원하는 값을 찾는 탐색 알고리즘입니다.
- **핵심 동작 방식**:

1. 배열의 중간 인덱스(`mid`)를 정합니다.
2. 중간값(`arr[mid]`)과 찾고자 하는 값(`target`)을 비교합니다.

- 중간값이 `target`과 같다면 탐색을 종료하고 위치를 반환합니다.
- 중간값이 `target`보다 크다면, `target`은 왼쪽에 있으므로 `right` 포인터를 `mid - 1`로 이동합니다.
- 중간값이 `target`보다 작다면, `target`은 오른쪽에 있으므로 `left` 포인터를 `mid + 1`로 이동합니다.

3. `left > right`가 될 때까지 값이 발견되지 않으면 탐색에 실패한 것(`-1` 반환)으로 처리합니다.

## 이진 탐색 코드

```javascript
function solution(arr, target) {
  let left = 0;
  let right = arr.size - 1; // 또는 arr.length - 1

  while (left <= right) {
    let mid = Math.floor((left + right) / 2);

    if (arr[mid] === target) {
      return mid;
    }

    if (arr[mid] > target) {
      right = mid - 1;
    } else {
      left = mid + 1;
    }
  }

  return -1;
}
```

## 이진 탐색과 순차 탐색

### 1. 이진탐색의 시간복잡도: $O(\log N)$

- **동작 원리**: 탐색할 때마다 탐색 범위가 **절반씩 감소**합니다 (길이가 1이 될 때까지 반복).
- **반복 횟수**: 전체 데이터 개수가 $N$개일 때, 루프는 약 $\log_2 N$번 돌게 됩니다.
- **시간복잡도 계산**: 내부 연산의 시간복잡도가 $O(1)$이므로, 전체 시간복잡도는 $O(1 \times \log N) = O(\log N)$이 됩니다.

### 2. 순차탐색 vs 이진탐색 비교

데이터 개수($N$)가 커질수록 두 방식의 성능 차이는 어마어마하게 벌어집니다.

| 데이터 개수 ($N$)             | 순차탐색 ($O(N)$) | 이진탐색 ($O(\log N)$) |
| ----------------------------- | ----------------- | ---------------------- |
| **4**                         | 4번               | 2번                    |
| **32**                        | 32번              | 5번                    |
| **128**                       | 128번             | 7번                    |
| **65,536**                    | 65,536번          | 16번                   |
| **약 42억** ($4,294,967,296$) | 약 42억 번        | **최대 32회**          |

- 약 42억 개의 원소가 있을 때, 순차탐색은 전부 다 체크해야 하지만 이진탐색은 **단 32번의 연산**만으로 값을 찾을 수 있습니다.

### 3. "한 번 거칠 때마다 후보가 절반으로 줄어든다"

이진탐색은 업다운 게임과 같습니다. 1부터 100까지의 숫자 중 하나를 맞출 때, 중간값인 50을 먼저 부르고 "업" 혹은 "다운"을 들으면 **남은 숫자의 범위가 정확히 절반으로 뚝 떨어집니다.**

이 과정을 단계별로 수식화해 보면 다음과 같습니다.

- **시작 (0단계)**: 전체 데이터 개수 $N$개
- **1번 지났을 때**: 남은 데이터 = $\frac{N}{2}$개
- **2번 지났을 때**: 남은 데이터 = $\frac{N}{2} \times \frac{1}{2} = \frac{N}{2^2}$개
- **3번 지났을 때**: 남은 데이터 = $\frac{N}{2^3}$개
- **...**
- **$k$번 지났을 때**: 남은 데이터 = $\frac{N}{2^k}$개

### 4. 왜 $\log N$이 될까? (수식으로 이해하기)

우리가 탐색을 계속하다 보면 결국 남은 데이터는 **1개**가 됩니다 ($(\frac{N}{2^k} = 1$). 이 식을 횟수인 $k$에 대해 정리해 보겠습니다.

1. $\frac{N}{2^k} = 1$
2. $N = 2^k$
3. 양쪽에 로그(log)를 취하면: **$k = \log_2 N$**

즉, 데이터가 1개 남을 때까지(즉, 원하는 값을 찾을 때까지) 걸리는 최대 횟수 $k$는 $\log_2 N$이 됩니다. 컴퓨터 과학에서는 밑수(2)를 생략하고 보통 $O(\log N)$이라고 부릅니다.

### 5. 구체적인 예시로 보기 ($N = 8$ 일 때)

데이터가 총 **8개** 있다고 가정해 봅시다. ($8 = 2^3$)

- **1단계**: 8개 $\rightarrow$ 4개 남음 ($8 / 2$)
- **2단계**: 4개 남음 $\rightarrow$ 2개 남음 ($4 / 2$)
- **3단계**: 2개 남음 $\rightarrow$ 1개 남음 ($2 / 2$)

8개의 데이터를 찾는데 **최대 3번**($\log_2 8 = 3$)의 연산이면 충분합니다.

만약 데이터가 **16개**($2^4$)라면?

- 4번 만에 끝납니다. ($16 \rightarrow 8 \rightarrow 4 \rightarrow 2 \rightarrow 1$)

데이터가 **1,024개**($2^{10}$)라면?

- 단 **10번** 만에 찾을 수 있습니다!

### 6. 이진탐색의 단점과 주의할 점

- **전제 조건**: 이진탐색은 반드시 **데이터가 정렬되어 있는 상태**여야만 사용할 수 있습니다.
- **정렬되지 않은 상태에서의 탐색**:
- 정렬부터 해야 하므로 퀵 정렬 같은 알고리즘을 쓰면 $O(N \log N)$의 시간이 추가로 소모됩니다.
- 최종 시간복잡도: $O(N \log N + \log N) \approx O(N \log N)$
- 이 경우, 정렬하는 과정 때문에 **오히려 순차탐색보다 더 느려질 수 있습니다.**

### 7. 요약 및 활용 팁

- **이진탐색을 써야 할 때**: 데이터가 이미 정렬되어 있거나, **탐색을 여러 번** 해야 하는 경우
- **굳이 쓸 필요 없을 때**: 데이터가 정렬되어 있지 않고, 딱 **한 번만 찾고 끝내거나** 최댓값 하나만 구하는 등 탐색 횟수가 적은 경우 (정렬 비용이 더 큼)
