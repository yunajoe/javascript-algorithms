## 📦 배열(Array)의 기본 연산과 시간 복잡도

배열은 데이터를 저장하는 가장 기본적인 자료구조이며, 주로 **삽입, 삭제, 탐색, 조회**의 기본 연산을 수행합니다. 시간 복잡도는 주로 **최악의 상황**을 기준으로 평가합니다.

---

### 1. 📥 삽입 (Insertion)

- **일반적인 위치(앞이나 중간)에 삽입할 때:**
- 새로운 데이터가 들어갈 자리를 만들기 위해 기존 데이터들이 한 칸씩 뒤로 이동해야 합니다.
- **시간 복잡도:** $O(N)$

- **맨 뒤에 삽입할 때:**
- 기존 데이터들의 이동이 전혀 필요 없으므로 바로 자리를 잡을 수 있습니다.
- **시간 복잡도:** $O(1)$

---

### 2. 🗑️ 삭제 (Deletion)

- **맨 앞의 값을 삭제할 때 (최악의 상황):**
- 맨 앞이 비게 되면서 나머지 $N-1개의 데이터가 모두 한 칸씩 앞으로 이동해야 합니다.
- **시간 복잡도:** $O(N)$

- **맨 뒤의 값을 삭제할 때:**
- 다른 데이터의 이동 없이 마지막 값을 바로 제거할 수 있습니다.
- **시간 복잡도:** $O(1)$

---

### 3. 🔍 탐색 (Search - 특정 값을 찾을 때)

- **원하는 값이 어디 있는지 모를 때:**
- 가장 간단한 방법은 처음부터 끝까지 모든 값을 하나씩 훑는 것입니다(순차 탐색). 만약 찾으려는 값이 맨 끝에 있거나 배열에 없다면 모든 요소를 전부 확인해야 합니다.
- **시간 복잡도:** $O(N)$

---

### 4. 🎯 $k$번째 원소 값 구하기 (조회/Access)

- **인덱스를 이용해 접근할 때:**
- 배열은 메모리에 연속적으로 배치되며 **인덱스(Index)** 기반으로 이루어져 있습니다. 따라서 $k-1$번째 인덱스를 곧바로 참조하면 중간 과정을 거치지 않고 한 번에 값을 가져올 수 있습니다.
- **시간 복잡도:** $O(1)$

```md
// 인덱스로 접근하면은 시간 복잡도가 왜 시간 복잡도 O(1) 일까?

### 1. 배열이 메모리에 저장되는 방식

컴퓨터의 메모리는 책꽂이에 책들이 일렬로 빽빽하게 꽂혀 있는 모습과 같습니다.

- **배열**은 이 메모리 공간에 **데이터들을 끊김 없이 '연속으로' 나란히** 꽂아두는 특성이 있습니다.
- 예를 들어, 시작 주소가 `100번지`이고 데이터 크기가 4바이트씩이라면:
- 1번째 값: `100번지`
- 2번째 값: `104번지`
- 3번째 값: `108번지`
- ... 이런 식으로 규칙적으로 배치됩니다.

---

### 2. 왜 한 번에 찾아갈 수 있을까? ($O(1)$의 비밀)

컴퓨터는 우리가 찾으려는 원소의 위치(주소)를 **수식 하나로 단 1번 만에** 계산해 낼 수 있습니다. (이것을 '임의 접근, Random Access'이라고 부릅니다.)

컴퓨터가 계산하는 공식은 다음과 같습니다:

> **(시작 주소) + (원하는 순서 $k - 1$) × (데이터 하나의 크기)**

- 만약 100번째 값을 찾고 싶다면, 컴퓨터는 하나씩 세어보는 게 아니라 `시작 주소 + (99 × 크기)`라는 계산을 단 **한 번** 딱 합니다.
- 계산이 끝나면 그 주소로 **순간 이동**해서 바로 데이터를 가져옵니다.

---

### 3. 만약 $O(N)$이 되려면 어떻게 해야 했을까?

- 만약 배열이 아니라 **연결 리스트(Linked List)** 같은 자료구조였다면, 첫 번째 원소부터 시작해서 화살표를 따라가며 1번, 2번, 3번... 일일이 거쳐서 100번째까지 걸어가야 합니다. 이때는 데이터 개수($N$)에 비례해서 시간이 걸리므로 $O(N)$이 됩니다.
- 하지만 **배열**은 주소를 수학 공식으로 바로 찍고 들어갈 수 있기 때문에, 데이터가 10개 있든 1,000만 개 있든 조회하는 데 걸리는 시간은 언제나 딱 1번(상수 시간)으로 똑같습니다.

### 💡 한 줄 요약

배열은 **"시작 주소에서 몇 번째 떨어진 곳인지 수학 공식으로 한 방에 계산해서 찾아갈 수 있기 때문에"** 데이터를 하나씩 거쳐 갈 필요가 없어 $O(1)$의 시간 복잡도를 가집니다!
```

---

### 💡 핵심 요약 표

| 연산 종류             | 상황별 특징                                | 시간 복잡도         |
| --------------------- | ------------------------------------------ | ------------------- |
| **삽입**              | 맨 뒤에 삽입 / 일반적인 위치(앞/중간) 삽입 | $O(1)$ / **$O(N)$** |
| **삭제**              | 맨 뒤 삭제 / 맨 앞 삭제 (최악)             | $O(1)$ / **$O(N)$** |
| **탐색**              | 처음부터 끝까지 값을 찾을 때               | **$O(N)$**          |
| **$k$번째 원소 조회** | 인덱스를 이용해 바로 접근할 때             | **$O(1)$**          |
