# 거품 정렬 (버블 정렬)

## 1. 거품 정렬이란?

- **개념**: 가장 단순한 형태의 정렬 알고리즘으로, 첫 번째와 두 번째 값을 비교하고, 두 번째와 세 번째 값을 비교하는 식(인접한 두 원소 비교)으로 끝까지 진행합니다.
- **동작**: 순서가 맞지 않은 값이 있다면 서로 자리를 교환하며, 이 과정을 정렬이 완료될 때까지 반복합니다.
- **특징**: 코드를 작성하기는 쉽지만, 상당히 비효율적이어서 성능 면에서는 다른 알고리즘에 비해 좋지 않다는 단점이 있습니다.

## 2. 시간 복잡도 ($O(N^2)$)

- 역순으로 데이터가 배치된 경우(예: `5 4 3 2 1`), 한 바퀴를 돌 때마다 데이터가 하나씩 제자리를 찾아가게 됩니다.
- $N$개의 데이터가 있다면 총 $N-1$번의 바퀴(순회)를 돌아야 모든 데이터가 제자리를 찾게 되므로, 각 바퀴를 돌 때마다 남은 원소들을 탐색하며 비교 및 교환 작업을 수행하므로, 전체 비교 횟수는 $N$에 비례하는 순회를 $N$번 반복하는 형태가 됩니다.
- 따라서 데이터의 개수 $N$이 늘어날수록 연산 횟수가 제곱에 비례하여 증가하므로 최종 시간 복잡도는 $O(N^2)$이 됩니다.

## 3. 개선된 버블 정렬 구현

교환 여부를 체크하는 플래그(`sorted`)를 매번 관리하는 대신, 데이터 개수(`len`)를 바탕으로 이미 정렬된 뒷부분은 반복문 범위에서 제외하여 효율적으로 처리하는 방식입니다.

```text
function bubble_sort(arr[])
  set len = arr.size

  for i = 0 ... i < len - 1
    for j = 0 ... j < len - 1 - i
      if arr[j] > arr[j + 1]
        set tmp = arr[j]
        arr[j] = arr[j + 1]
        arr[j + 1] = tmp

  return arr

```

# 선택 정렬

**선택 정렬(Selection Sort) 핵심 개념**

- **정의:** 다른 정렬 알고리즘보다 더 일상적인 방법의 알고리즘입니다.
- **주요 아이디어:**

1. 전체 값 중 가장 작은 값을 찾습니다.
2. 해당 값을 맨 첫 번째에 배치합니다.
3. 첫 번째 값을 제외하고 가장 작은 값을 찾아 두 번째에 배치합니다.
4. 두 번째, 세 번째, ... $n-1$번째 값을 제외하고 가장 작은 값을 찾아 정해진 위치에 배치합니다.

**시간복잡도**

- 중간에 정렬된 상태라도 종료될 수 있는 거품 정렬과는 다르게, 선택 정렬의 경우 반드시 $n-1, n-2, \dots, 2, 1$번의 비교 연산을 수행해야 하므로 약 $\frac{n \times (n - 1)}{2}$번의 연산을 필요로 합니다.
- 따라서 시간복잡도는 $O(N^2)$이 됩니다.
